name: fastnotes_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  # Network
  dio: ^5.8.0+1
  # Dependency Injection
  get_it: ^8.0.3
  # State Management
  provider: ^6.1.5
  # Icons
  font_awesome_flutter: ^10.8.0
  # UI Components
  flutter_slidable: ^4.0.0
  go_router: ^16.0.0
  hive: ^2.2.3
  google_sign_in: ^6.3.0
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
